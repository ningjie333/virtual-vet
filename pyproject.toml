[project]
name = "virtual-vet"
version = "0.1.0"
description = "Veterinary clinical diagnosis education game"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.21.0",
    "flask>=2.3.0",
    "matplotlib>=3.5.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
    "ruff>=0.4.0",
]
